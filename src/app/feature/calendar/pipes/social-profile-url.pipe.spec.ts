import {SocialProfileUrlPipe} from './social-profile-url.pipe';

describe('SocialProfileUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialProfileUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
