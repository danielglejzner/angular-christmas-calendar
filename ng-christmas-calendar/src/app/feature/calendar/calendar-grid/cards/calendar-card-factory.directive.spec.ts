import { CalendarCardFactoryDirective } from './calendar-card-factory.directive';

describe('CalendarCardFactoryDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarCardFactoryDirective();
    expect(directive).toBeTruthy();
  });
});
