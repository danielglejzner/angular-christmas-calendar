import { TwitterUrlPipe } from './twitter-url.pipe';

describe('TwitterUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new TwitterUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
